import './globals.css';

export const metadata = {
  title: 'Adaa Consulting',
  description: 'Strategic consulting solutions for modern businesses',
};

export default function RootLayout({ children }) {
  return children;
}
